spring:
  datasource:
    #    각 환경에 맞는 디비 생성 후 사용
    url: jdbc:mariadb://${testDbIp:127.0.0.1}:3306/kchlibaray_test?useUnicode=true&characterEncoding=utf8&autoReconnect=true&serverTimezone=Asia/Seoul
    username: ${testDbId:root}
    password: ${testDbPw:rlaclgns5628}
  jpa:
    hibernate:
      ddl-auto: create